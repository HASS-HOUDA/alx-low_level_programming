#include<stdio.h>
/**
 * main - print single digit numbers of base ten
 * Return: Always 0 (success)
*/
int main(void)
{
	int n;
	{
		for (n = 0; n <= 9; n++)
			printf("%d", n);
	}
	putchar('\n')
	return (0);
}

